mixin video(video)
  h4
    a(href=`/videos/${video.id}`)=video.title
  ul 
    li #{video.author}
    li #{video.rating}/5
    li #{video.comments} comments
    li #{video.createdAt}
    li #{video.views}